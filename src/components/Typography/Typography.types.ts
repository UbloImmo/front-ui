import type { TypographyColor } from "../../types";

export type TypographyProps = {
  color?: TypographyColor;
  italic?: boolean;
  underline?: boolean;
  overline?: boolean;
  lineThrough?: boolean;
  bold?: boolean;
};
