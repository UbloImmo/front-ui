import { Meta } from "@storybook/blocks";
import { ComponentInfo, Canvas, ComponentProps, Markdown } from "../../../docs/blocks"
import { Content } from "../../../docs/containers"
import * as LoadingStories from "./Loading.stories.tsx";
import {Loading} from "./Loading.component.tsx";
import * as LoadingAnimations from "./animations";
import { objectKeys } from "@ubloimmo/front-util";

<Meta of={LoadingStories} />

<ComponentInfo of={LoadingStories} />

<Content>

## Properties

<ComponentProps of={LoadingStories} />

## Usage

### Choosing an animation

This component currently offers {objectKeys(LoadingAnimations).length} animations (<Markdown inline>{objectKeys(LoadingAnimations).map((name) => `\`${name}\``).join(', ')}</Markdown>) for you to choose from.


</Content>