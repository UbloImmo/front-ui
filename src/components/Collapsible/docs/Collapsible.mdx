import { Meta } from "@storybook/blocks";
import { ComponentInfo, Canvas } from "@docs/blocks";
import { Content } from "@docs/containers";
import * as CollapsibleStories from "../Collapsible.stories.tsx";

<Meta title="Components/Collapsible/Usage" />

<ComponentInfo of={CollapsibleStories} figmaId="2583-10008" />

<Content>

## Usage

`Collapsible` is a component that functions similarly to an accordion, allowing users to expand and reveal its nested content by clicking on it.

> Collapsible components can be nested to create a tree-like structure of expandable content. You can nest Collapsible components as many levels deep as you need.

## Custom content

TO DO

</Content>
