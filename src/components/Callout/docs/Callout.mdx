import { Meta } from "@storybook/blocks";
import { ComponentInfo, Canvas } from "@docs/blocks";
import { Content } from "@docs/containers";
import * as CalloutStories from "../Callout.stories.tsx";

<Meta title="Components/Feedbacks/Callout/Usage" />

<ComponentInfo of={CalloutStories} figmaId="1672-2110" />

<Content>

## Usage

`Callout` is useful to provide feedback to the users. It can be used to display general information, warnings or instructions related to a form.

## Colors

To enhance user experience, the `Callout`'s state is visually represented through the `color` property, allowing the user to easily identify whether the message is a warning, an additional information, or an alert.
The `color` property is set by default to `primary`.

<Canvas of={CalloutStories.Colors} />

## Icon

### Using a custom icon

`Callout` uses an [Icon](?path=/docs/components-icon-usage--docs) by default that illustrates the state of the component (determined by the `color` property). This helps visually inform the user about the nature of the information.

This icon is rendered with the optional `icon` property set by default to `auto`.

However, if you want to customize the icon further, you can replace the default icon by providing a valid `IconName` in this `icon` property.

> Please refer to the [Icon Repository](?path=/docs/components-icon-repository--docs) for a complete list of `IconName`.

<Canvas of={CalloutStories.WithCustomIcon} />

### Removing the icon

You can remove the icon displayed in `Callout` by setting the `icon` property to `null`

<Canvas of={CalloutStories.WithoutIcon} />

## Label

`Callout` render its text with the **required** `label` property.

<Canvas of={CalloutStories.Labels} />

## Adding a title

You can add an extra **title** to your `Callout`'s text, using the optional `title` property.

<Canvas of={CalloutStories.WithTitle} />

## Sizes

`Callout` has two sizes: `m` and `l`.

`Callout` most common usage with it's default size `m` is to display a warning or an information in a form.

The `l` size is used to display a warning or an information that needs more attention in a page.

<Canvas of={CalloutStories.Sizes} />

## Content

The `children` property accepts both simple text strings and other content, including components from this library, HTML elements, or custom components.

This allows you to create entirely custom content within `Callout`.

> The example below uses [Hypertext](?path=/docs/components-hypertext-usage--docs) and [FlexBox](?path=/docs/layouts-flex-usage--docs) components as a content.

<Canvas of={CalloutStories.WithCustomChildren} />

</Content>
