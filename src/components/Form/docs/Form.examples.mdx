import { Meta } from "@storybook/blocks";
import { ComponentInfo, Canvas, Callout } from "@docs/blocks";
import { Content } from "@docs/containers";
import * as FormStories from "../Form.stories.tsx";
import { Icon } from "@/components";

<Meta title="Components/Form/Examples" />

<ComponentInfo of={FormStories} figmaId="2407-16053" />

<Content>

## Examples

As our Form component gets more and more customizable and richer,
it gets harder and harder to truly convey just how many different ways it can be used.

> Take a look at the _Show code_ button under the each of the following examples to wrap your head around the various [property combinations](?path=/docs/components-form-properties--docs), their usage and their result.

### A form tailored for creation

By setting only a handful of properties, one can simplify the creation process for all users.

<Canvas of={FormStories.CreatableForm} />

### You got tables ?

We got tables <Icon name="EmojiSmile" />

<Canvas of={FormStories.Table} />


### Custom fields

<Canvas of={FormStories.CustomFields} />

### Custom content

<Canvas of={FormStories.CustomContent} />


### A form as a modal

See the black `close` button in the top right corner while in "read" mode ? No ?
Yeah... _We still need to work on adding that and figuring out how to make it play nice with the rest of the UI._

<Canvas of={FormStories.AsModal} />

<Callout color="warning" icon="ExclamationTriangle">
  This variant is not fully implemented yet and remains at an experimental state.
</Callout>

### Steps within a form ??

_Has science gone too far ?_
<br />
**Yes.** It does _not_ work (_yet_).

<Canvas of={FormStories.Steps} />

<Callout color="error" icon="ExclamationTriangle">
  This is just the silly experiment of a mad scientist. Do not try at home.
</Callout>

### All possible fields

This form showcases all the different first-party fields that can be used within the Form component.

<Canvas of={FormStories.AllFields} />

</Content>