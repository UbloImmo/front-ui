import { Meta } from "@storybook/addon-docs/blocks";
import {
  ComponentInfo,
  Canvas,
  ComponentProps,
  ComponentList,
} from "@docs/blocks";
import { Content } from "@docs/containers";
import * as InputStories from "../Input.stories.tsx";
import * as inputsIndexRaw from "@components/Input";

<Meta title="Components/Forms/Input/Usage" />

<ComponentInfo of={InputStories} figmaId="76%3A6562" />

<Content>

## Usage

Inputs are interactive value fields, commonly used in a form pattern, allowing users to enter data.

Input's specific value type can be set with the `type` property.

<Canvas of={InputStories.Types} />

### Specific Input Components

<ComponentList
  index={inputsIndexRaw}
  parent="Forms/Input"
  exclude={["Input", "IconPickerInput", "ComboBoxInput"]}
  columns={3}
/>

> All input types share common properties such as `required`, `disabled`, `error`, and `placeholder`.
>
> <br />
> [Checkout Common Input properties](?path=/docs/components-forms-input-common-properties--docs)

## Controls

All inputs can be managed on their state, appearance, and interactions with the users.

### Disable

Inputs are active by default. Deactivate an input, and prevent the user from entering any data, can be done with the property `disabled`.

When set to true, the input become read-only, and is grayed out to inform users that they can't enter value there.

<Canvas of={InputStories.Disabled} />

### Error

Controlling the validity of an input can be made with the `error` property.

When set to true, the input's style changes (red border color) and alert visually the user that the entered data is invalid.

<Canvas of={InputStories.Error} />

### Placeholder

While the placeholder property is optional, including it in the input field is useful for guiding users on the expected value. This placeholder remains visible until users enter a value.

<Canvas of={InputStories.Placeholder} />

</Content>
