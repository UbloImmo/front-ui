import { Meta } from "@storybook/blocks";
import { ComponentInfo, Canvas } from "@docs/blocks";
import { Content } from "@docs/containers";
import * as SelectInputStories from "../SelectInput.stories.tsx";

<Meta title="Components/Input/SelectInput/Usage" />

<ComponentInfo of={SelectInputStories} />

<Content>

## Usage

`SelectInput` component is a specific type of [Input](?path=/docs/components-input-usage--docs), used to prompt the user to select an option from a list.

### Disabling options

To prevent user from selecting a specific Option, you can set it to disabled.

### Searching among the options

To allow users to type the search option, you can set the property

</Content>
