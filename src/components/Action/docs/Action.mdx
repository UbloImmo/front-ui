import { Meta } from "@storybook/blocks";
import { ComponentInfo, Canvas } from "@docs/blocks";
import { Content } from "@docs/containers";
import * as ActionStories from "../Action.stories.tsx";

<Meta title="Components/Actions/Action/Usage" />

<ComponentInfo of={ActionStories} figmaId="761-4208" />

<Content>

## Usage

### Sizes

Action provides 4 sizes (`default`, `centered`, `large`, & `card`) to help users differentiate between multiple actions.

<br />
*As expected, the default size is `default`.*

> Actions are set to fill their container width-wise (`width: 100%`).

<Canvas of={ActionStories.Sizes} />

### Basic customization

Action buttons can have their `label`, `icon`, and `badgeLabel` properties customized to fit most needs.

#### Label

There are no hard limits to the Action's label. Be wary of very long labels as they could be clipped on smaller window sizes.

> Action labels are allowed to wrap up to 2 lines before getting clipped.

<Canvas of={ActionStories.Labels} />

#### Icon

Actions internally use a [StaticIcon](?path=/docs/components-iconography-staticicon-usage--docs) to render their Icon.
As such, they can take any valid `IconName`.

> Please refer to the [Icon Repository](?path=/docs/components-iconography-icon-repository--docs) for a complete list.

<Canvas of={ActionStories.Icons} />

#### Color

Actions use the `primary` color by default. Destructive actions may use the `error` color.

> Hovering over an action will reveal its color.

<Canvas of={ActionStories.Colors} />

### Further customization

#### Badge

Provide a `badgeLabel` to display a [Badge](?path=/docs/components-states-badge-usage--docs) alongside the Action's label.
Its color will change between `primary` and `gray` depending on the Actions's `disabled` property.

> No `Badge` will be rendered if `badgeLabel` is an empty string or missing.

<Canvas of={ActionStories.BadgeLabels} />

#### Tooltips

Add a [Tooltips's properties object](?path=/docs/components-feedbacks-tooltip-properties--docs) as its `iconTooltip` property to display a tooltip inside the action.

> Any children passed to the `iconTooltip` property will be ignored so as to always render an icon

<Canvas of={ActionStories.Tooltip} />

#### Description

Use the `description` property to display additional text below the action's label, providing users with more context or details about the action's purpose.

> The description is only shown in `large` & `card` sized actions. 

<Canvas of={ActionStories.Description} />

#### Indicator

Use the `indicator` property to display an additional element on the top right corner of the static icon.

> Since `centered` actions display a simple [Icon](?path=/docs/components-iconography-icon-usage--docs) instead of a [StaticIcon](?path=/docs/components-iconography-staticicon-usage--docs), the `indicator` property is ignored.

<Canvas of={ActionStories.Indicator} />

### Disabled

Actions are inherently buttons with a bit of _flair_. Therefore, they can be disabled.

<Canvas of={ActionStories.Disabled} />

</Content>
